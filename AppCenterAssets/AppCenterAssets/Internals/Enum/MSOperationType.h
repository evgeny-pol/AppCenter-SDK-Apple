
typedef NS_ENUM(NSString, MSOperationType) {
    MsOperationTypeCopy = @"Error closing IO resources when copying files.",
    MsOperationTypeRead = @"Error closing IO resources when reading file.",
    MsOperationTypeWrite = @"Error closing IO resources when writing to a file.",
    MsOperationTypeDefault = @"Error closing IO resources."
};
